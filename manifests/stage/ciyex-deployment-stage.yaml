apiVersion: apps/v1
kind: Deployment
metadata:
  name: ciyex-app-stage
  labels:
    app: ciyex-app-stage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ciyex-app-stage
  template:
    metadata:
      labels:
        app: ciyex-app-stage
    spec:
      containers:
        - name: ciyex-app-stage
          image: hinikubestageacr.azurecr.io/ciyex-app-stage:v1.0.4
          ports:
            - containerPort: 8080  # Spring Boot
            - containerPort: 3000  # EHR UI (Next.js)
            - containerPort: 3001  # Portal UI (Next.js)
            - containerPort: 3002  # Admin UI (Next.js)
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "stage"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/ciyexdb"
            - name: SPRING_DATASOURCE_USERNAME
              value: "postgres"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
            - name: NEXT_PUBLIC_API_URL
              value: "https://stg.ciyex.com/api"

