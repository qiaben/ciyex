apiVersion: v1
kind: Service
metadata:
  name: ciyex-app-stage
spec:
  type: ClusterIP
  ports:
    - name: spring
      port: 8080
      targetPort: 8080
    - name: nextjs
      port: 3000
      targetPort: 3000
  selector:
    app: ciyex-app-stage