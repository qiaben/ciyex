package com.qiaben.ciyex.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebSocket configuration removed due to dependency issues.
 * Using polling-based real-time updates instead.
 */
@Configuration
public class WebSocketConfig {
    // WebSocket functionality moved to polling-based approach
}