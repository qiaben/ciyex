apiVersion: apps/v1
kind: Deployment
metadata:
  name: ciyex-app
spec:
  template:
    spec:
      containers:
        - name: ciyex-app
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
